name: Build Android APK

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    # 环境变量：版本号和构建时间
    env:
      VERSION_NAME: "1.0.0"
      BUILD_TIME: ${{ github.run_number }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # 获取完整 git 历史，用于生成版本号

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Generate version info
      id: version
      run: |
        # 获取短提交哈希
        SHORT_SHA=$(git rev-parse --short=7 HEAD)
        # 获取当前日期时间
        BUILD_DATE=$(date +'%Y%m%d')
        # 构建版本号
        VERSION="${VERSION_NAME}.${BUILD_TIME}+${BUILD_TIME}.${SHORT_SHA}"
        APK_VERSION="v${VERSION_NAME}-${BUILD_DATE}-${SHORT_SHA}"
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "apk_version=$APK_VERSION" >> $GITHUB_OUTPUT
        echo "short_sha=$SHORT_SHA" >> $GITHUB_OUTPUT
        echo "build_date=$BUILD_DATE" >> $GITHUB_OUTPUT
        echo "Generated Version: $APK_VERSION"

    - name: Grant execute permission for gradlew
      run: |
        cd MBBridgeController
        chmod +x gradlew

    - name: Build Debug APK
      run: |
        cd MBBridgeController
        ./gradlew assembleDebug --no-daemon

    - name: Build Release APK
      run: |
        cd MBBridgeController
        ./gradlew assembleRelease --no-daemon

    - name: Rename APKs
      run: |
        APK_VER="${{ steps.version.outputs.apk_version }}"
        # 重命名 Debug APK
        mv MBBridgeController/app/build/outputs/apk/debug/app-debug.apk \
           MBBridgeController/MBBridgeController-${APK_VER}-debug.apk
        # 重命名 Release APK
        mv MBBridgeController/app/build/outputs/apk/release/app-release.apk \
           MBBridgeController/MBBridgeController-${APK_VER}-release.apk
        echo "Renamed APKs to: MBBridgeController-${APK_VER}-[debug|release].apk"

    - name: Verify renamed APKs
      run: |
        APK_VER="${{ steps.version.outputs.apk_version }}"
        echo "=== Checking APK files ==="
        ls -lh MBBridgeController/*.apk
        echo ""
        echo "=== File names ==="
        basename MBBridgeController/*.apk

    - name: Upload Debug APK
      uses: actions/upload-artifact@v4
      with:
        name: MBBridgeController-${{ steps.version.outputs.apk_version }}-debug
        path: MBBridgeController/MBBridgeController-${{ steps.version.outputs.apk_version }}-debug.apk
        retention-days: 30

    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: MBBridgeController-${{ steps.version.outputs.apk_version }}-release
        path: MBBridgeController/MBBridgeController-${{ steps.version.outputs.apk_version }}-release.apk
        retention-days: 90

    - name: Get APK info
      run: |
        APK_VER="${{ steps.version.outputs.apk_version }}"
        DEBUG_APK="MBBridgeController-${APK_VER}-debug.apk"
        RELEASE_APK="MBBridgeController-${APK_VER}-release.apk"
        SHORT_SHA="${{ steps.version.outputs.short_sha }}"
        BUILD_DATE="${{ steps.version.outputs.build_date }}"

        echo "### 📱 MBBridge Controller 构建完成" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**构建信息:**" >> $GITHUB_STEP_SUMMARY
        echo "- 版本: \`${{ steps.version.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 提交: \`${SHORT_SHA}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 日期: \`${BUILD_DATE}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 构建号: \${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**📦 APK 文件:**" >> $GITHUB_STEP_SUMMARY
        echo "- Debug: \`${DEBUG_APK}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 大小: $(du -h MBBridgeController/${DEBUG_APK} | cut -f1)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "- Release: \`${RELEASE_APK}\`" >> $GITHUB_STEP_SUMMARY
        echo "- 大小: $(du -h MBBridgeController/${RELEASE_APK} | cut -f1)" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**📥 下载说明:**" >> $GITHUB_STEP_SUMMARY
        echo "1. 点击下方 Artifacts 区域的文件链接" >> $GITHUB_STEP_SUMMARY
        echo "2. 下载的是 zip 压缩包" >> $GITHUB_STEP_SUMMARY
        echo "3. **解压后**才能得到 APK 文件" >> $GITHUB_STEP_SUMMARY
        echo "4. 解压后的文件名包含完整版本信息" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "📍 下载位置: Actions -> Artifacts" >> $GITHUB_STEP_SUMMARY
